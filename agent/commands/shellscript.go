package commands

import (
	//"duov6.com/agent/core"
	"fmt"
)

func ShellScript(from string, name string, data map[string]interface{}, resources map[string]interface{}) {

	fmt.Println(data)

	//agent = resources["agent"].(*core.Agent)

}
