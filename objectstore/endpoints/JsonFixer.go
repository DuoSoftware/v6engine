package endpoints

import (
	"fmt"
	//"strings"
)

func AAA(input []byte) (output []byte) {
	fmt.Println("Before Converting : " + string(input))
	//if strings.Contains(, "'")
	fmt.Println("After Converting : " + string(output))
	return
}
